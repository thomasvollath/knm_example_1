<%= form_for(@pvendor) do |f| %>
  <% if @pvendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pvendor.errors.count, "error") %> prohibited this pvendor from being saved:</h2>

      <ul>
      <% @pvendor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
